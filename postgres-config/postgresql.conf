# PostgreSQL configuration for Debezium CDC
# This configuration enables logical replication required for Debezium

# Connection settings
listen_addresses = '*'
port = 5432
max_connections = 100

# Memory settings
shared_buffers = 128MB
dynamic_shared_memory_type = posix

# Write-ahead logging (Required for Debezium CDC)
wal_level = logical
max_wal_senders = 10
max_replication_slots = 5

# Logging
log_statement = 'none'
log_min_duration_statement = -1

# Locale and formatting
datestyle = 'iso, mdy'
timezone = 'UTC'
lc_messages = 'en_US.utf8'
lc_monetary = 'en_US.utf8'
lc_numeric = 'en_US.utf8'
lc_time = 'en_US.utf8'

# Default configuration
default_text_search_config = 'pg_catalog.english'

# Remove the problematic include_dir line
# include_dir = 'conf.d'  # This was causing the error
